import {expect} from "chai";



describe('Sample test suite', () => { 

    it('can run a sample test', async () => {

        const sampleVariable = 2;
    
        expect(sampleVariable).to.be.eq(2);
    });
    
})

